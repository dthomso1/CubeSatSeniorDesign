<UserControl x:Class="CubeSatCommSim.View.ErrorSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CubeSatCommSim.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Name="ErrorGrid" AutoGenerateColumns="False"
                  ItemsSource="{Binding ErrorList}">



            <DataGrid.Columns>
                <DataGridCheckBoxColumn
                    Header=""
                    Binding="{Binding isSelected}"
                />
                <!-- Binding="{Binding ErrorsSelected}" -->

                <DataGridTextColumn Binding="{Binding id}"
                                Header="ID" />
                <DataGridTextColumn Binding="{Binding description}"
                                Header="Description" />
                <DataGridTextColumn Binding="{Binding isFatal}"
                                Header="isFatal" />
                <DataGridTextColumn Binding="{Binding ModuleAffected}"
                                Header="Module Affected" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

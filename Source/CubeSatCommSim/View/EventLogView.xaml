<UserControl x:Class="CubeSatCommSim.View.EventLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CubeSatCommSim.View"
             xmlns:vm="clr-namespace:CubeSatCommSim.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
      <vm:ViewModel_EventLog x:Key="eventList"/>
      <DataTemplate x:Key="EventTemplate">
        <TextBlock Text="{Binding Path=Log}"/>
      </DataTemplate>
    </UserControl.Resources>
    <Grid>
      <StackPanel
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Name="EventLogPanel">
        <ListBox ItemsSource="{Binding EventLogger}"
                 ItemTemplate="{StaticResource EventTemplate}"/>
      </StackPanel>
    </Grid>
</UserControl>

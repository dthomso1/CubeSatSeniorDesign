<UserControl x:Class="CubeSatCommSim.View.InternalSimView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
             xmlns:local="clr-namespace:CubeSatCommSim.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

  <Grid>
    <Grid.Resources>

    </Grid.Resources>
    <DataGrid x:Name="dataGrid1">
      <DataGrid.Columns>
        <DataGridTextColumn Header="id" Binding="{Binding XPath=@id}" />
        <DataGridTextColumn Header="isFatal" Binding="{Binding XPath=@isFatal}" />
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
